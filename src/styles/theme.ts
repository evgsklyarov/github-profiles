const colors = {
  mainBg: '#fff',
  
  mainText: '#000',
  secondaryText: '#666',
};

const borderRadius = {
  small: '5px',
  medium: '10px',
  circle: '50%',
};

const size = {
  sm: '576px',
  md: '768px',
  lg: '992px',
  xl: '1200px',
}
const media = {
  sm: `(min-width: ${size.sm})`,
  md: `(min-width: ${size.md})`,
  lg: `(min-width: ${size.lg})`,
  xl: `(min-width: ${size.xl})`,
}

export default {
  colors,
  borderRadius,
  media,
};
