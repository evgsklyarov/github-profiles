import styled from 'styled-components';
import { Title as T, Paragraph as P } from '~/components/Text';

const Title = styled(T)`
  margin-bottom: 20px;
`;

const Paragraph = styled(P)`
  margin-bottom: 20px;
`;

export { Title, Paragraph };
