import styled from 'styled-components';
import { Action } from '~/components/Button';

export const Pagination = styled.div`
  display: flex;
  align-items: center;
  justify-content: center;
`;

export const PaginationAction = styled(Action)`
  width: fit-content;
`;
